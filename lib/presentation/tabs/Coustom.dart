import 'package:flutter/material.dart';

class CoustomOrder extends StatelessWidget {
  const CoustomOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: Colors.brown);
  }
}
